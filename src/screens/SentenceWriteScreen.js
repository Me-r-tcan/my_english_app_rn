import React from "react";
import { StyleSheet, Text, View } from "react-native";

const SentenceWriteScreen = () => {
  return (
    <View>
      <Text>SentenceWriteScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default SentenceWriteScreen;
